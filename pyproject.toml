[project]
name = "personal-blog"
version = "0.1.0"
requires-python = ">=3.12"

[tool.vercel]
entrypoint = "backend.app:app"

[tool.vercel.scripts]
build = "cd frontend && npm install && npm run build:prod"
